# Checkpoints

Large weight files are intentionally not tracked in git (GitHub 100MB limit / LFS quota):

- `epoch_latest.pth` (~103MB)
- `normal-model-vitl16_384.onnx` (~1.3GB)

Keep these files locally under `checkpoint/` when running demos.
